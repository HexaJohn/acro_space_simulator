{
  // Pinned Flutter master SDK (FVM-managed — see .fvmrc). Keeps this project
  // off the global stable install; flutter_scene requires master.
  // Written by `fvm use`; run it after changing .fvmrc.
  "dart.flutterSdkPath": ".fvm/versions/84af28a0646eb00c06b5556be0d28e5e32cdf1c7",
  "search.exclude": {
    "**/.fvm": true
  },
  "files.watcherExclude": {
    "**/.fvm": true
  }
}