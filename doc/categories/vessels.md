Spacecraft + aircraft: the `Vessel` aggregate, `Stage`/`Part`, `Engine` +
`JetEngine`, the `PartCatalog`, and `VesselAssembler` (placed parts → one rigid
body with mass, CoM, and parallel-axis inertia). Staging, gimbal, RCS, wings.
